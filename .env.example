# Server-side only. Never expose this value with a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=

# Optional: use a compatible Responses API model for all Roundtable passes.
ROUNDTABLE_MODEL=gpt-5
# PARALLEL_MODEL is accepted temporarily for existing deployments.
